# taplo — TOML formatter/linter (https://taplo.tamasfe.dev).
# Build artifacts contain generated Cargo.tomls; never format them.
exclude = ["target/**"]

[formatting]
align_entries = false
column_width = 100
indent_string = "    "
reorder_keys = false
